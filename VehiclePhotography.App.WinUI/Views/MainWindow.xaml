<!-- Copyright (c) Microsoft Corporation and Contributors. -->
<!-- Licensed under the MIT License. -->

<Window
    x:Class="VehiclePhotography.App.WinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    xmlns:data="using:VehiclePhotography.App.Domain.Values"
    mc:Ignorable="d">

    <controls:GridView x:Name="VehicleGridView" ItemsSource="{x:Bind _vehicles}">
        <controls:GridView.ItemTemplate>
            <DataTemplate x:DataType="data:VehicleInfo">
                <StackPanel>
                    <Image Source="{x:Bind ThumbnailPath}" Width="100" Height="100"/>
                    <TextBlock Text="{x:Bind Make}" />
                    <TextBlock Text="{x:Bind Model}" />
                    <TextBlock Text="{x:Bind Year}" />
                    <TextBlock Text="{x:Bind Vin}" />
                    <TextBlock Text="{x:Bind VehicleType}" />
                </StackPanel>
            </DataTemplate>
        </controls:GridView.ItemTemplate>
    </controls:GridView>

</Window>
